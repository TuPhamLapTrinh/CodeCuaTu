#include<bits/stdc++.h>
#include<windows.h>
using namespace std;
// CAD
main()
{
    ifstream file;
    file.open("F:/:))/cad.txt",ios_base::in);
    string line;
    while (!file.eof())
    {
        getline(file , line);
        cout<<line<<"\n";
        Sleep(50);

    }
}
